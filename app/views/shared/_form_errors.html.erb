<% if record.errors.any? %>
  <div>
    <h2><%= t("shared.errors.heading", count: record.errors.count) %></h2>
    <ul>
      <% record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
